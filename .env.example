# Google Cloud Storage CLI Testing Configuration
# Copy this file to .env and fill in your values

# REQUIRED: Your Google Cloud Project ID
GCS_PROJECT_ID=your-project-id

# REQUIRED: Your GCS bucket name for testing
# Make sure this bucket exists and you have read/write permissions
GCS_BUCKET_NAME=your-test-bucket

# OPTIONAL: Prefix for test files (default: "test-")
# All test files will be created with this prefix for easy identification and cleanup
GCS_TEST_FILE_PREFIX=test-

# OPTIONAL: Path to Google Cloud credentials JSON file
# Only needed if using service account authentication
# If not set, will use default gcloud auth
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json